---
title: TSConfig 预设
description: '@cat-kit/tsconfig 提供 Node、Bun、Web、Vue 的 TypeScript 配置预设'
outline: deep
---

# TSConfig 预设

`@cat-kit/tsconfig` 提供一组可直接 `extends` 的 TypeScript JSON 预设，覆盖通用基础配置、Node.js、Bun、Web 和 Vue 3。它是编译期依赖，不参与运行时产物。

## 页面导航

- [预设说明](./presets)
