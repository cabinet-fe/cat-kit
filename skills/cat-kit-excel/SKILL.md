---
name: cat-kit-excel
description: >
  Documents @cat-kit/excel streaming read/write and workbook model types. Use when parsing or generating .xlsx, streaming large spreadsheets, or typing Excel cell/row APIs.
---

# @cat-kit/excel

## 安装

```bash
npm add @cat-kit/excel
```

## 查证 API

[`generated/`](generated/) 下 **`.d.ts`** 与 npm `dist` 一致；入口 [`generated/index.d.ts`](generated/index.d.ts)。子模块路径因 `preserveModules` 可能较深，见 [references/](references/)。

**运行环境**：通用（按具体 API 在浏览器或 Node 使用）。

## 更多

- [references/](references/)
- [examples.md](examples.md)
