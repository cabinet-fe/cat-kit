---
name: cat-kit-be
description: >
  Documents @cat-kit/be Node.js utilities (fs helpers, logger, LRU cache, scheduler, system info). Use when building backends with @cat-kit/be or Node-specific file and infra helpers.
---

# @cat-kit/be

## 安装

```bash
npm add @cat-kit/be
```

## 查证 API

[`generated/`](generated/) 下 **`.d.ts`** 与 npm `dist` 一致；入口 [`generated/index.d.ts`](generated/index.d.ts)。

**运行环境**：仅 Node.js（使用 `node:` 内置模块等）。

## 更多

- [references/](references/)
- [examples.md](examples.md)
