# 为 agent-context 文档首页增加生命周期 Mermaid 图

## 补丁内容

在文档首页增加一张 Mermaid 流程图，直观展示 Agent Context Workflow 的生命周期，帮助用户在入口页快速理解 `init`、`plan`、`replan`、`implement`、`patch`、`rush`、`done` 之间的关系。

## 影响范围

- 修改文件: `docs/content/packages/agent-context/index.md`
