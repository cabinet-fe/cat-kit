# 为 Tween 与 Virtualizer 补充文档示例

## 补丁内容

为 `@cat-kit/fe` 新增的 `Tween` 与 `Virtualizer` 补充可直接运行的文档示例，并将示例通过 `::: demo` 嵌入对应文档页，确保读者不只看到 API 片段，还能在文档站中直接观察 Tween 的逐帧补间效果与虚拟列表的局部渲染行为。

## 影响范围

- 新增文件: `docs/examples/fe/tween/basic.vue`
- 新增文件: `docs/examples/fe/virtualizer/basic.vue`
- 修改文件: `docs/content/packages/fe/tween.md`
- 修改文件: `docs/content/packages/fe/virtualizer.md`
