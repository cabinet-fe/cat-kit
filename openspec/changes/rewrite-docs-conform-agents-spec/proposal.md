# Change: 重写所有包文档使其符合 docs/AGENTS.md 规范

## Why

当前 `docs/content/packages/` 下的所有包文档结构不符合 `docs/AGENTS.md` 定义的规范：

- `index.md` 页面包含了大量 API 说明、安装步骤、快速开始等内容，违反"仅介绍 + 导航"规则
- 功能页（非 index.md）缺少规定的固定二级标题结构（`## 介绍`、`## 快速使用`、`## API参考`）
- 部分 demo 容器放置位置不正确
- **文档内容与实际包导出不符**（API 名称错误、不存在的函数等）
- **过度使用 Emoji 图标**（规范要求：默认不用，必要时一页最多 0-2 个）

统一文档结构可提升可维护性和用户导航体验。

## What Changes

### 1. index.md 重构（6 个包）

将以下包的 `index.md` 精简为"介绍 + 导航"结构：

- `core/index.md` - 当前 435 行，需精简到约 30-50 行
- `http/index.md` - 当前 109 行，需精简到约 20-30 行
- `fe/index.md` - 当前 82 行，需精简到约 20-30 行
- `be/index.md` - 当前 272 行，需精简到约 30-40 行
- `excel/index.md` - 当前 210 行，需精简到约 20-30 行
- `maintenance/index.md` - 当前 171 行，需精简到约 30-40 行

### 2. 功能页结构调整（约 28 个文件）

为所有功能页添加固定二级标题结构：

- `## 介绍` - 功能简介
- `## 快速使用` - 最小可用示例
- `## API参考` - 完整 API 文档

受影响文件清单：

- `core/`: `data.md`, `data-structure.md`, `date.md`, `env.md`, `optimize.md`, `pattern.md`
- `http/`: `client.md`, `plugins.md`, `types.md`
- `fe/`: `file.md`, `storage.md`, `virtualizer.md`, `web-api.md`
- `be/`: `cache.md`, `config.md`, `fs.md`, `logger.md`, `net.md`, `scheduler.md`, `system.md`
- `excel/`: 需新增功能页或保持当前结构（单页包）
- `maintenance/`: `build.md`, `deps.md`, `guide.md`, `monorepo.md`, `release.md`, `version.md`

### 3. 修复文档与实际功能不符的问题

根据 `packages/<pkg>/dist/index.d.ts` 核实并修正以下问题：

**core 包**（差异最大）：

| 文档错误 | 实际导出 | 修正 |
|---------|---------|------|
| `$arr.chunk/unique/flatten` | `arr()` 函数 | 使用 `arr()` |
| `$obj.pick/omit/cloneDeep` | `o()` 函数 | 使用 `o()` |
| `$date.format/parse/addDays` | `Dater` 类 + `date()` | 使用 `Dater/date()` |
| `$num.clamp/range/random` | `$n` + `n()` | 使用 `$n/n()` |
| `Timer` 类 | 不存在 | 删除相关内容 |
| `Observer` 类 | `Observable` | 使用 `Observable` |
| `safe()` | `safeRun()` | 使用 `safeRun()` |
| 无 | `Validator` 相关 | 新增验证器文档 |

**fe 包**：

| 文档错误 | 实际导出 | 修正 |
|---------|---------|------|
| `saveFile()` | `saveFromBlob/saveFromStream/saveFromURL` | 更新 API |
| `Store` 类 | `IDB` 类 | 使用 `IDB` |

**http 包**：
- 删除文档中提到但不存在的插件
- 仅保留 `TokenPlugin` 和 `MethodOverridePlugin`

### 4. 移除过度 Emoji

当前 Emoji 使用情况：
- `be/index.md`: 📁⚙️📝💾🌐📊⏰ （7个，需减少到 0-2）
- `fe/index.md`: 🗄️📁📋🎯📜 （5个，需减少到 0-2）
- `maintenance/index.md`: 🔍📊🔢📦🚀 （5个，需减少到 0-2）

规范要求：**默认不用 Emoji，必要时一页最多 0-2 个**

### 5. 内容迁移

将 `index.md` 中的详细内容迁移到对应功能页：

- 安装指南 → 各包首个功能页或 guide.md
- API 示例 → 对应功能页的"快速使用"和"API 参考"部分
- Demo 容器 → 移至功能页（如已在功能页则保留）

## Impact

- **Affected specs**: 无现有 OpenSpec 规范
- **Affected code**: `docs/content/packages/` 下所有 `.md` 文件（约 34 个）
- **Breaking**: 无代码破坏性变更；文档 URL 结构保持不变
- **工作量估算**: 约 6-8 小时编写工作（含 API 核实）
