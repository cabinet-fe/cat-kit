---
'@cat-kit/agent-context': patch
---

Optimize generated ac-workflow Skill as a canonical Agent Skills bundle: abstract structured questioning away from host tool names, install `.agents/skills/ac-workflow` as the canonical source with optional compatibility entries, add `skill-eval`, and make `prompt-gen` use a generic default profile.
