{
  "name": "@cat-kit/http",
  "version": "1.0.3",
  "description": "HTTP 请求工具",
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "lint": "bunx oxlint .",
    "test": "bunx vitest run --globals",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./src": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "dependencies": {
    "@cat-kit/core": "workspace:^"
  },
  "devDependencies": {
    "@cat-kit/tsconfig": "workspace:*"
  }
}
