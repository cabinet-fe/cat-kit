import {  } from './optimize'

describe('优化', () => {
  it('并发控制', async () => {
    // const queue: number[] = []
    // await concurrent(
    //   [
    //     500, 180, 400, 300, 200, 150, 700, 300, 200, 100, 700, 600, 500, 200,
    //     200, 190, 330
    //   ],
    //   async (delay) => {
    //     await new Promise((rs) => {
    //       setTimeout(() => {
    //         rs(delay)
    //       }, delay)
    //     })

    //     queue.push(delay)
    //   },
    //   5
    // )

    // expect(queue.length).toBe(17)
  })
})
