# Go 代码风格指南

## 文件命名

遵循**snake_case**命名法，例如：`user_service.go`

## 项目约定

### 包组织

- `internal/`：项目内部包
- `pkg/`：可被外部导入的包
- `cmd/`：可执行程序入口

### 函数参数

- 超过 5 个参数时使用选项模式或配置结构体

### 错误处理

- 使用 `%w` 包装错误，保留错误链
